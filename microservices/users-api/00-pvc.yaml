apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-api-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50M
  storageClassName: default
  selector:
    matchLabels:
      name: users-api-pv